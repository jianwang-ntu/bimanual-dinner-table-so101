__pycache__/
*.pyc
.DS_Store
MUJOCO_LOG.TXT

# rendered perception datasets -- regenerate with scripts/make_perception_dataset.py
data/*.npz
# ...except the ten evaluation frames: they are 231 KiB, they are what the
# headline numbers are quoted on, and scripts/bench_openvino.py needs them to
# run from a clean clone.
!data/perception_eval10.npz

# the trained ACT checkpoint -- 160.8 MB, over GitHub's file limit. The
# demonstrations it was trained on ARE committed (data/demos/), so
# `python3 scripts/train_act.py` rebuilds it from a clean clone.
models/act_policy.pt
